# Keep the Python test toolchain deterministic in CI without changing the
# application's production dependency ranges.
pytest==8.4.2
pytest-asyncio==0.26.0
pytest-cov==5.0.0
httpx==0.27.2
fakeredis==2.37.0
