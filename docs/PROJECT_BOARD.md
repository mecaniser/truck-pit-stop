# DieselBridge Delivery Board

Last consolidated: 2026-08-11. This board was reconstructed from DieselBridge
task histories, the repository, GitHub, CI, and Railway production evidence.
House Cleanup is complete. The former dirty tree remains recoverably preserved
on `codex/house-cleanup-preservation`; its intentional outcomes have been rebuilt,
reviewed, merged, and released. Reconcile this board whenever newer evidence exists.

## Operating rules

- One item has one accountable owner, even when several roles contribute.
- `Done` requires merge/release and acceptance evidence; “implemented in a task”
  is not sufficient.
- Keep `codex/house-cleanup-preservation` as recovery evidence until the product
  owner deliberately archives it; it is not an active implementation branch.
- New requests enter Inbox through Product & Delivery Lead.

## In progress

| ID | Priority | State | Outcome | Owner | Evidence now | Next gate |
|---|---|---|---|---|---|---|
| DB-045 | P0 | Security correction implemented — fresh exact-SHA gates pending | Deliver an immutable Inventory activity foundation, complete selected-part lifecycle hub, and staff-only repair-first counter sales with reservations, manual/Stripe/QuickBooks Payments, partial returns/refunds, QuickBooks Accounting sync, and net reporting | Backend & Integrations | Product approved one integrated high-risk release on branch `codex/db045-inventory-lifecycle-counter-sales` from exact merged DB-044 base `cb09d157a1e2635d53847539ca3e7ffa9bfb7965`. Architecture froze contract `docs/contracts/db045-inventory-lifecycle-counter-sales.md` version `1.0.2`, 763 lines, SHA-256 `ec7aeb53cb89ba12b464734e622ed78087863e1c8532fbf00463979f88351a25`; 1.0.2 is a formatting-only whitespace correction with no semantic contract change. The integrated implementation was committed as `6df6ae400fef46aaac136779f3fa775eb727c5af`; the first frozen candidate `ab054666759931ebf6920960666120d2793811a6` received Security NO-GO `0/1/0` for provider-outbox tenant/aggregate validation and QA NO-GO `0/1/1` for missing QuickBooks sandbox acceptance plus committed whitespace findings. Backend corrected every counter-sale outbox path to validate tenant-scoped aggregates, envelope identifiers, related sales, and provider accounts before provider I/O; malformed/cross-tenant events terminate DEAD with a safe generic error. Five adversarial cross-tenant event families assert zero Stripe, QuickBooks, or email calls and unchanged foreign aggregates. Focused evidence after correction: backend contract/regression `53/53`; true PostgreSQL concurrency/immutability/backfill `2/2`; frontend components `42/42`; isolated Playwright `2/2` across `1440/1280/960/390/320`; production build, changed-source ESLint, compile, Alembic single head, and base-to-candidate `git diff --check` pass; Impeccable detector `[]`. Disposable PostgreSQL proved fresh upgrade, DB-045 downgrade/upgrade, protected financial-row downgrade refusal, append-only triggers, cross-tenant FK rejection, and two identical backfill reconciliations with the second inserting zero events. Signed local runtime serves the DB-045 frontend on `5173` and isolated DB-045 API on `8045`; Activity, lifecycle, and Parts sales load successfully. No production payment, migration, backfill, gate enablement, deployment, or shared-service/data mutation occurred. Provider readiness remains incomplete because configured QuickBooks Payments/Accounting environments are production rather than sandbox; no real QuickBooks financial action is authorized. | Commit the bounded correction, obtain fresh independent Security and QA decisions on the new frozen candidate SHA, then open the integrated PR and require protected CI. Release and merge remain held until QuickBooks sandbox charge/refund/reconciliation evidence exists; after merge, deploy, migrate/backfill, reconcile, enable the global and target-tenant gates, and verify production without a real customer charge. |
| DB-044 | P2 | Merged — post-merge CI green | Bring Parts & Inventory and Purchasing back into the shared application design language while preserving service-manual geometry inside ledger internals only | Frontend & UX | Exact seven-file candidate committed as `a8339c1ebaef092d44745efde17c8521cfbdcd2c`, pushed to `codex/db044-inventory-design-coherence`, and opened as PR `#305`. All six protected PR contexts passed. PR `#305` was squash-merged at `2026-08-27T03:39:33Z` as main `cb09d157a1e2635d53847539ca3e7ffa9bfb7965`; DB-045 is based exactly on that merge. Post-merge Quality gates run `33037023183` completed `success` on the merge SHA: Migration graph, Frontend checks, Frontend full suite, Backend tests, Backend full suite, and Playwright smoke all passed. Focused Parts/Purchasing components passed `33/33`; changed-source ESLint, production build, `git diff --check`, and Impeccable detector `[]` passed before delivery. | Preserve as the merged DB-045 prerequisite; Release retains the deployment/runtime record without reopening DB-044 scope. |
| DB-043 | P2 | Ready to ship — Product authorized commit, push, PR, and merge | Adapt My Shop navigation to the available workspace width so laptop layouts prioritize the active Shop surface instead of reserving a second full-height rail | Frontend & UX | Implemented one shared semantic Shop navigation on branch `codex/db043-shop-navigation-adaptation` from base `2ea7272023af1b2faba205b782b8a01682e6bc7e`. The unchanged operational order Services, Labor Book Time, Inventory, Purchasing stays grouped left while Team/Analytics remain a separated secondary group. The same DOM becomes a contained two-column grid below `42rem`, a flat content-width horizontal subnav from `42rem` through laptop containers, and a narrowed `208px` flat vertical rail only when the My Shop container reaches `100rem`; routes, icons, labels, active state, keyboard order, Enter activation, 44px targets, role/tenant behavior, and compact states remain unchanged. The former duplicate mobile/desktop navigation trees, staggered fade-in, opacity staging, and `transition-all` are removed. Focused My Garage components pass `2/2`; the dedicated isolated Shop-navigation Playwright regression passes `1/1` across `1920`, `1440`, `1280`, `960`, `390`, and `320` widths; production build, exact changed-source ESLint, `git diff --check`, and Impeccable layout detector `[]` pass. One signed-in localhost browser batch verified route activation, `44px` for all six targets, correct operational/secondary separation, and no document overflow at `1024x768`, `1280x800`, `1440x900`, exact `1716x986`, ultrawide `1920x1080`, and `390x844`, including expanded/collapsed staff-rail states where available. Exact `1716x986` keeps the Shop menu horizontal with `1401px` workspace width under the expanded staff rail; `1920x1080` with `1744px` workspace correctly adopts the `208px` vertical fallback. Browser console errors were zero. Stable localhost `5173` is HTTP 200 from `com.dieselbridge.db043-vite`, PID `2939`, rooted at the DB-043 frontend. Scope remains frontend presentation only with no backend, API, auth, tenant, data, migration, dependency, worker, flag, or infrastructure change. | Commit and push the focused DB-043 candidate, open its pull request, require protected PR CI, then squash-merge under Product's explicit authorization. No manual deployment or production mutation is authorized. |
| DB-042 | P2 | Ready to ship — Product authorized commit, push, PR, and merge | Restore and streamline selected-part editing in the Parts inspector, including photo add/replace/remove and editable part identity/details, using the existing inventory mutation and photo contracts | Frontend & UX | The existing shared `QuantityStepper` owns all four Add Part stepper-intent fields with zero native number inputs. The selected-part header now leads with identity/status and one quiet `Edit details` control; the former four-action header cluster is removed. Stock owns the single explicit `Adjust stock` entry point while Reorder remains editable at its fact. Ordering owns the only contextual Purchasing route and purchase-preparation action: an actionable server recommendation receives oxide primary emphasis and covered stock retains a neutral manual action. The completed Ordering distillation separates supplier facts from the 44px Edit action, exposes Part no./Pack/Minimum/Lead time/Last cost as labeled facts, explains that purchase preparation stages a review in Purchasing, replaces the action with a prepared state and `View purchase list` route after staging, and condenses empty open-order/receipt history into two explicit `None` rows under Purchase activity. Populated PO/receipt records, role/archive restrictions, supplier validation/concurrency, and the existing inventory, supplier, purchase, photo, API, auth, tenant, data-model, migration, dependency, worker, and infrastructure contracts remain unchanged. Focused Parts components pass `27/27`; exact changed-source ESLint, `git diff --check`, and Impeccable detector `[]` pass. Signed-in localhost verification on the DB-042 runtime confirms the desktop hierarchy, a semantic supplier record with a separate 44px Edit source action, two 43px `None` activity rows, 44px purchase-preparation action, 390px containment with `scrollWidth=clientWidth` and no document overflow, and zero console errors. Port `5173` is HTTP 200 from Vite PID `1121` under `com.dieselbridge.db042-vite` rooted at the DB-042 frontend. | Create the focused PR from the frozen DB-042 candidate, require protected PR CI, then merge under Product's explicit authorization; record merge and release evidence. |
<!-- DB-041 release gate, 2026-08-25: PR #295 head 4d071047 failed only the Railway schema-validation step because Railway's live schema moved to JSON Schema 2020-12 while CI still forced draft-07. Alembic remained linear at head 126 and the repository readiness check passed. Product & Delivery Lead reproduced `railway.json valid` with ajv-cli draft2020 and added the one-line CI compatibility correction; Release must gate the resulting new PR head and all rerun contexts before merge. -->
| DB-041 | P2 | Implementation committed and pushed — PR #295 open; fresh independent QA GO; awaiting Release exact-SHA merge gate | Add deterministic server-side Parts ledger sorting that composes with search, row-inclusion filters, infinite pagination, and ID-stable selection, while restoring the unmerged DB-039 Shop grouping | Frontend & UX | Contract v1 remains frozen at Architecture fingerprint `f1f228ee2fec46b1364027ee2932c30125d5b9e33e57500455a218ffd27654c9` against exact base `86c9fc648a9f96d7b978da1208ce37b76a8d7ca3`. Backend provides all six server sorts with explicit directions, stable pagination tie-breakers, null-last bins, numeric cost/actionable urgency, and preserved tenant/role/response behavior; focused API/read tests pass `18/18`, PostgreSQL-dialect compiles pass `12/12`, and the behavioral matrix passed `1/1` after migration to head on a fresh isolated PostgreSQL 15 database. Frontend provides semantic desktop headers, compact-only sort fallback, pagination reset, focus restoration, and inspector/checked-ID continuity; combined components pass `25/25`, production build, exact ESLint, `git diff --check`, and Impeccable detector `[]` pass. The previously QA-approved but unmerged DB-039 Shop grouping is carried forward byte-for-byte: Services, Labor Book Time, Inventory, Purchasing remain contiguous, with Team and Analytics in the separated lower cluster. Backend fingerprint is `70aa4338be78dea76818bbabc0be6734f0ae1bfb8735a82e034dc3874cc3e6a5`; Frontend source manifest is `f69c5bb730f19b2ed467800a248733f3914bd203038bfa3bdb30ddec17ccce9d`. The original independent source/fixture gate returned `P0/P1/P2=0/0/0`, but Product later reproduced a mixed-runtime acceptance failure: DB-041 Vite on `5173` was paired with retired DB-038 API code on `8000`, so Available direction was ignored and Bin/Average cost returned `422`. Release corrected only the API code mount to the DB-041 backend, preserving the DB-041 frontend and leaving PostgreSQL, Redis, migrations, flags, and data unchanged. Product & Delivery Lead then verified the existing authenticated local tab against the live API: Available asc/desc began `0,0,0` versus `120,120,111,102,97`; Bin asc/desc began `3B,A1,A1,A2` versus `Z2,Z2,Z2,Z2,Y2,X1`; Average cost desc began `1800.00,1597.76,1102.64,1057.88,1003.75` and asc began `0.00`; all live Parts requests returned `200` with no alert or `422`, and Catalog order was restored. QA amended the false P1/candidate-mismatch report to no reproduced defect and `P0/P1/P2=0/0/0`. Product's bounded follow-up is now implemented: Movement performs one eager `limit=1` count read before activation while its ledger remains lazy and paginated, Search and the Stock filter group share one explicit `46px` outer control height with `44px` targets, desktop exposes direct Comfortable/Compact density controls plus a conditional Catalog-order reset, and compact widths retain the full directional Sort dialog without a desktop Options dropdown. Focused Parts and DB-039 carry-forward components pass `25/25`; production build, exact frontend ESLint, `git diff --check`, Playwright discovery `1/1`, and the final Impeccable layout detector `[]` pass. Product & Delivery Lead verified the existing signed-in `http://localhost:5173/dashboard/garage/inventory` tab after HMR: the initial tab read `Movement 428`; Search, Stock filters, and Density each computed to `46px`; both density targets computed to `44px`; desktop Options was absent; Compact and Comfortable updated `aria-pressed` and the body density class; Average cost exposed the direct `Reset to catalog order` control; reset removed it; and no alert/runtime error appeared. This remains Product/primary live evidence, not the independent gate. Fresh independent QA then adopted exact tracked implementation fingerprint `91bc0dae88df287b1916fc0d1acaa40ebdbb18818634597740968bd76eb8b448` before this board-only reconciliation and ran the exact isolated DB-041 Playwright configuration `1/1` across `1440/1280/1100/960/760/390/320`, with zero runtime errors and no residual `5181` listener; QA verdict is GO with `P0/P1/P2=0/0/0`. Product then authorized delivery: implementation commit `e67858b23b0147ac67555ea2df673d6842d3c3ac` was pushed on `codex/db041-parts-ledger-sorting`, and focused PR [#295](https://github.com/mecaniser/truck-pit-stop/pull/295) opened against `main`; no merge, deployment, service, migration, flag, or shared-data mutation was performed. | Release & Reliability owns the independent exact-PR-head merge gate. Merge and deploy remain unperformed and require their normal exact-SHA/CI evidence. |
| DB-040 | P2 | Released | Distill Parts & inventory into one coherent ledger-and-inspector operating surface without changing product behavior or data contracts | Frontend & UX | Exact candidate `01803ee944f1a0617aa8de4827799f58d0d118ab` received independent QA GO with `P0/P1/P2=0/0/0`. PR #294 merged as `86c9fc648a9f96d7b978da1208ce37b76a8d7ca3`; Railway production release was verified. The release remained frontend-only with no backend/API/auth/data/migration/dependency change. | Closed. Further Parts ledger sorting/filtering is DB-041 and requires its own implementation and fresh gates. |
| DB-003 | P1 | Done — reconciled with DB-035 and released | Finish versioned additional-work authorizations without regressing the authenticated garage cockpit | Backend & Integrations | PR #272 squash-merged the reconciled DB-003/DB-035 stack as `a600eb1314630a51be8420f9cee043bc1406ac28` after independent Security and QA GO and all six protected contexts passed. The linear `118_authenticated_presentation` → `119_ro_projection_cardinality` migration completed successfully. Initial Railway deployments were production web `38583e84-ec0f-4284-80c4-8a9654325434`, performance web `3fe87a9d-af60-4f77-a75e-194a8425a043`, and worker `169e18ad-130e-4342-be75-9708832d51e1`, all `SUCCESS`; homepage, API health, readiness, deployment logs, and worker scans were clean. A late primary-QA NO-GO found that the committed DB-003 Playwright config lacked current Vite runtime identity. Focused candidate `de589a858ab2d5cd8895abd9c33df0789e99ff07` corrected only the test harness, passed exact clean-start browser acceptance `11/11` in `18.6s` with ambient identity unset, and received fresh fallback QA GO with `P0/P1/P2=0/0/0`. PR #273 passed all six protected contexts and squash-merged as `9c4fe946f4544fed5f807ab382612bb807a328df` at `2026-08-23T08:11:05Z`. Correction deployments production web `58965e63-98c0-435f-853b-1141cbbcdf65` and performance web `b3d86fca-2d9d-44d1-a99e-a0742ecbf418` are `SUCCESS` on `9c4fe94`; the worker correctly remained unchanged and healthy on `a600eb1`. Final canary `2026-08-23T08:12:35Z–08:12:36Z` returned HTTP 200 for homepage, `/health`, and `/health/ready`, with clean web startup/error scans. No rollback was required and no authenticated journey was fabricated for the test-only correction. | Closed. Future changes to additional-work authorization, DB-035 workstation behavior, or migration 119 require a new board item and fresh proportional Security/QA gates. |
| DB-015 | P0 | Frontend release authorized | Remediate WebSocket token logging | Security & Identity | Backend PR #259 merged as `277146526dd9b7f95d4ae6fe09dd683d558eb304` after all six protected CI contexts passed. Railway production web `b6881963-0864-43b2-9e7c-2f5c8396943a`, performance web `aa9052b9-649d-446d-afac-965125cf00f0`, and worker `5c73e3f2-02e0-462a-9597-334ccf019455` reached healthy state with repeated 200 probes and no 5xx, worker traceback, task failure, or credential value in the correlated application logs. Independent real-browser QA proved a legacy HttpOnly-cookie login and old `?token` client open/ping-pong against the new backend with the query value absent from logs. The only authorized production tab was a token-null WorkOS session: its 2026-08-11 reload remained authenticated, refreshed, loaded data, and produced zero console/log credential leaks, but the old frontend intentionally did not attempt WebSocket without a client token. Product explicitly accepted that production legacy-session limitation as covered by the exact pre-production compatibility gate; it was not run in production and must not be reported otherwise. | Ship the protected queryless frontend PR. After deployment, require the existing WorkOS session to produce a queryless staff WebSocket connection and ping/pong, bounded refresh/logout behavior, zero console/network/edge/application credential leakage, healthy web services, and unchanged worker deployment `5c73e3f2-02e0-462a-9597-334ccf019455`; roll back the frontend if any post-deploy gate fails. |
| DB-032 | P2 | Ready to Release — independent QA GO | Animate the public DieselBridge wordmark on first page load | Frontend & UX | On `codex/db032-logo-motion`, exact application candidate `ee2b6e2698a60c9965ac9afd2d15c0541431253a` makes the bridge construct deck → pillars → arch across 1.18s while the twelve fixed-slot letters settle over 0.82s with deterministic 0.34–0.68s delays. The SVG box never moves, the footer remains static, and reduced motion uses a 120ms no-travel fade. Evidence: focused landing tests `27/27`, full frontend `173/173`, production build, exact changed-source ESLint, and diff-check passed; dedicated DB-032 Playwright `7/7` passed with natural-load sampling across 32 frames at 1366/390/320, zero clipped or colliding letters, fixed bridge bounds, all three ordered path animations, static footer, and no browser/runtime failures. Current midpoint captures are under `e2e/test-results/db032-natural-letter-drop-frame-{1366,390,320}.png`. Product received a fresh independent QA GO on exact `ee2b6e2`. No API, auth, tenant, data, backend, dependency, worker, or migration change. | PR #263 must carry this exact candidate plus the focused QA handoff, pass every protected CI context, and remain unmerged until Product gives new explicit release authority for the superseding motion change. No deployment is authorized. |
| DB-035 | P2 | Released — Repair Orders control alignment deployed | Reshape and polish the authenticated garage cockpit so the landing promise is fulfilled by connected real workflows | Frontend & UX | Candidate `4f58e6bf3cd2aa1996e16e7d846130231fbce9b9` received independent QA GO with `P0/P1/P2=0`: exact 44px search/status geometry, shared 184px selected-record action/status width and right edge, intact focus/targets, and preserved compact/mobile overrides. Focused Repair Orders/Dashboard tests passed `49/49`, full frontend passed `263/263`, production build and all six protected checks passed. PR #270 squash-merged as `261b5759eecf08fbcec5191f1f96dd1c05decfc2`; Railway production deployment `d9e979e5-20ec-4931-8180-9a6e306bea08` is `SUCCESS`. The live production CSS asset contains both shared geometry tokens and their selected-record rules; web/API health returned HTTP 200. No API, data, auth, tenant, permission, mutation, dependency, or migration change. | Release can close the visual canary when an authenticated staff browser is available: measure the selected-record desktop view and compact/mobile view, confirm equal heights/right edges and clean browser errors. The available controlled browser redirected to `/login`, so authenticated post-deploy geometry was not fabricated. Rollback is PR #270 only. |
| DB-035A | P0 | Released with DB-035 | Replace the rejected transition-only Shop Work → Repair Orders topology with one persistent canonical repair-order workbench: Shop Work is today's tenant-local navigator; Repair Orders is all-record discovery. | Backend & Integrations | Included in PR #264 / `4cbaec940c8ed6bb7b7bdf04950f5a5eb3d11c89`; independent gates and protected CI passed. The Truck Pit Stop tenant-wide `new` presentation rollout is active with the production readback recorded in DB-035. | Complete the shared authenticated Truck Pit Stop staff canary; rollback remains the DB-035 tenant default only. |
| DB-035B | P2 | Released with DB-035 | Make the new-presentation authenticated rail describe work, management, and communication rather than one undifferentiated destination list. | Frontend & UX Recovery Owner | Included in PR #264 / `4cbaec940c8ed6bb7b7bdf04950f5a5eb3d11c89`; independent gates and protected CI passed. The Truck Pit Stop tenant-wide `new` presentation rollout is active with the production readback recorded in DB-035. | Complete the shared authenticated Truck Pit Stop staff canary; rollback remains the DB-035 tenant default only. |
| DB-035C | P0 | Released with DB-035 | Replace the new-presentation customer Sidekick primary detail with a canonical page-owned customer workstation while preserving legacy rollback. | Frontend & UX Recovery Owner | Included in PR #264 / `4cbaec940c8ed6bb7b7bdf04950f5a5eb3d11c89`; independent gates and protected CI passed. The Truck Pit Stop tenant-wide `new` presentation rollout is active with the production readback recorded in DB-035. No backend/API/business-logic/auth change was introduced by DB-035C. | Complete the shared authenticated Truck Pit Stop staff canary; rollback remains the DB-035 tenant default only. |
| DB-037 | P1 | Done — merged, locally healthy, production release verified | Provide one MVP local controller that switches fixed ports 5173/8000 between clean registered worktrees without mixed frontend/backend sources and shows `branch@shortSHA` in development. | Release & Reliability | Exact application candidate `613e9a97638ae7565cba81586de15a303bb605fa` received fresh independent Security GO and QA GO with `P0/P1/P2=0`; controller `39/39`, frontend critical `81/81`, full frontend `259/259`, production build, Playwright smoke, and all six protected checks passed. Focused PR #265 was squash-merged to main as `f289d6743e4ca18318abe70c73234df63a909b72`; origin/main ancestry matches. Railway deployment `79424ad3-4a6a-4d52-a064-809d32ca3b8f` completed SUCCESS for that exact commit, and `www.dieselbridge.com` plus `api.dieselbridge.com/health` returned HTTP 200. The controller-owned local runtime remains healthy on fixed ports `5173/8000` from one clean DB-037 worktree at `9ca7e5f5e2676c710a42cb53804118b9cc41905a`, using preserved `truckpitstop_db035` at sole Alembic head/current `118_authenticated_presentation`; PostgreSQL/Redis and data were preserved. | Closed. Use `dieselbridge-local` for future explicit clean-worktree switches. DB-037 remains local tooling; authenticated-presentation rollout/activation is a separate DB-035 release action. |
| DB-038 | P1 | Backend migration linearization complete; fresh Architecture/Security/QA re-gates pending | Turn DieselBridge Inventory into connected Parts & inventory and Purchasing workspaces that preserve current capabilities while adding supplier-source intelligence and grouped purchasing. | Backend & Integrations | Prior candidate-`125_inventory_supplier_sources` Security/QA and migration evidence is retained as historical evidence only. After GitHub main `ba1560cde0b7800c436166863a5f35e3be95f993` introduced `125_invoice_ets_invoiced_at`, Release merged main conflict-free as `4571f7362d83566f87724e400dc714f6f029c061`, producing two heads. Architecture returned GO for identity-only linearization: DB-038 is now `126_inventory_supplier_sources` directly after main `125_invoice_ets_invoiced_at`; all upgrade/downgrade operations and constraint names remain byte-identical below the revision header. Fresh disposable PostgreSQL 15.18 passed empty `-> 126`, `126 -> 125_invoice_ets_invoiced_at -> 126`, and `126 -> 124_parts_operations_v1 -> 126`; downgrading only DB-038 preserved `invoices.ets_invoiced_at` and `ix_invoices_ets_invoiced_at`. Backfill evidence created exactly one same-tenant exact source, created none for cross-tenant or ambiguous/unmatched legacy inputs, linked the exact matching PO line, and left the supplier-mismatched legacy line nullable. All four supplier-source provenance constraints were present. Full focused PostgreSQL passed `23/23`; focused API/read-contract passed `16/16`; Alembic reports sole head `126_inventory_supplier_sources`. The uniquely named disposable container/listener was terminated after evidence and shared `5432` remained unchanged. No invoice/report behavior, endpoint, frontend/harness, shared runtime, production, flag, customer data, push, PR, merge PR, deploy, reset, cleanup, or unrelated checkout change occurred. | Backend must commit the bounded migration/test/contract/board correction locally after final compile/diff identity checks, then Product routes the exact commit to fresh Architecture, independent Security, independent QA, and Release freeze. DB-038 remains unapproved. |

DB-038 localhost recovery evidence: the shared API was mounted from exact candidate `f992f5b2771625287caac9c244b8b32e2f511052`, but its configured database was `truckpitstop_db038_preview` at migration 124 while current code required 126, producing live 500s for parts, purchase orders, returns, and cores. Release first preflighted and backed up the similarly named inactive `truckpitstop` database at migration 125 as `/private/tmp/truckpitstop-before-db038-126-20260824T231735Z.dump`; after the API database name was resolved, Alembic's execution log proved the active database actually upgraded `124_parts_operations_v1 -> 125_invoice_ets_invoiced_at -> 126_inventory_supplier_sources`, and an immediate validated post-upgrade archive was recorded at `/private/tmp/truckpitstop_db038_preview-after-126-20260824T231735Z.dump`. Therefore no pre-upgrade archive of the active API database exists from this recovery and that deviation remains explicit. The active DB now reports sole head 126, all supplier columns and `inventory_supplier_sources` exist, and correlated authenticated requests for summary, parts/detail, purchase orders, returns, and cores return HTTP 200. Only Vite 5173 was restarted with visible identity `codex/db038-ux-recovery@f992f5b`; authenticated hard-refresh canaries render Inventory with 1,119 matching parts and Purchasing with no retry/error state. No seed, reset, cleanup, other-service restart, push, PR, deploy, production, flag change, or manual business-data mutation beyond the authorized migration/backfill occurred.

DB-038 inventory-controls recovery evidence: on parent `c505250b6f8235192deb4bc93af3ff97bb8dbac3`, the selected Inventory detail now exposes a role-gated inline editor for On hand, On order, Reorder level, and Current WAC. Stock changes require and submit an adjustment reason; unchanged fields are omitted; saves are single-flight; API failures retain the recoverable draft; cancel and selected-row changes discard stale drafts; successful saves keep the selected record and refresh inventory/activity projections. Focused components pass `24/24`; the isolated DB-038 Playwright gate passes `3/3` across 1280/960/390/320 with exact payload, validation, role, failure-retention, selection-reset, and containment assertions; TypeScript, production build, exact/changed lint, Impeccable detector (`[]`), and diff integrity pass. Fresh independent QA is still required before release action.

DB-038 independent QA returned exact candidate `a529e7ab370c357164d3bbfb3c5e423acfa4ff98` NO-GO (`P0/P1/P2=0/0/1`) because selected Inventory detail rendered literal `undefined` when `unit_type` was absent. The focused Frontend correction now renders `No unit specified` for undefined, null, or empty unit metadata and pins the behavior in component and Playwright assertions at desktop and compact widths. Focused components remain `24/24`; the exact DB-038 Playwright gate remains `3/3` across 1280/960/390/320; exact/changed lint, TypeScript, production build, Impeccable detector (`[]`), and diff integrity pass. Fresh independent QA is required on the remediation SHA.

DB-038 Product reopened UX acceptance on exact candidate `b496c502a22e8456bb06b5624be89d0581589061` after the functional QA GO because the Inventory surface exposes implementation language, gives equal weight to too many controls, defaults to an editable archived catalog record, conflates physical stock, incoming supply, replenishment policy, and accounting cost, and renders the complete 1,119-row catalog after eager cross-workspace collection loading. Frontend & UX owns the focused recovery. Acceptance: use repair-shop language; remove internal release/catalog/ledger copy; exclude archived records from the default active-stock view and prevent accidental editing; separate quantity, stocking-policy, and cost semantics while preserving role gates and stock-adjustment reasons; make attention queues actionable; bound rendered rows and load collections only for the active work area; preserve keyboard, 44px, responsive, empty/error, and tenant-safe API behavior. Scope is frontend presentation/query orchestration only—no backend, API contract, migration, permission, flag, production, or customer-data mutation. Fresh independent QA at 1280/960/390/320 is required before release returns to consideration.

DB-038 operator-language recovery implementation evidence: the Inventory workspace now defaults to active parts, provides an explicit read-only Archived view, uses shop language (`Available now`, `Incoming`, `Reorder at`, `Average unit cost`), separates quantity adjustment, reorder-point, and average-cost actions, keeps role gates and the required available-stock adjustment reason, links the reorder and purchase-order counts to their working queues, and presents human-readable inventory history. Collection requests are activated only for the selected work area and matching Inventory results render in 50-row increments. Focused components pass `26/26`; the isolated DB-038 Playwright gate passes `3/3` across 1280/960/390/320 with active/archived behavior, bounded rendering, separated payloads, role/validation/failure retention, and responsive containment assertions; exact and changed frontend lint, TypeScript, production build, Impeccable detector (`[]`), visual screenshot inspection, and diff integrity pass. No backend, API, migration, permission, flag, production, or data change is included. Fresh independent QA remains required on the resulting exact candidate before push, PR, merge, or deploy consideration.

DB-038 frozen-harness gate correction: Release executed unchanged harness blob `13745d749512f3c9128d4d73e6d849ceb06b0ceb` and returned `2/3`; the sole failure was a strict-locator collision because non-exact `Stock` heading lookups also matched the adjustment-form heading. Frontend & UX changed only the four repeated harness locators to exact accessible-name matching. Discovery passes with exactly `3 tests in 1 file`. The local exact Playwright run is unverified because this managed sandbox denied the configured fresh-server bind at `127.0.0.1:5181` with `listen EPERM`; no assertion or product failure was observed. Corrected frozen harness blob is `7f14f036b42ca3e3e6fbbca26e7a1a66149c05fc`. No product code, contract, backend, migration, runtime, flag, production, or customer-data change is included. QA remains held until Release adopts this exact blob, completes the focused run, and supplies a fresh complete fingerprint.

DB-038 independent QA returned frozen candidate fingerprint `1d4095859e596dc8bf53c54daac68dc3c370f6a50df628219e8d62d3aa92c681` NO-GO (`P0/P1/P2=0/1/0`) because the 1280px My Shop composition clipped the selected part's essential Stock labels and values inside an intrinsically over-wide master/detail grid. Frontend & UX made only the Parts workbench layout container-aware: the ledger can shrink without forcing the inspector outside its owner, the inspector receives a bounded readable minimum, and its Stock facts stack when the actual inspector—not the viewport—is narrow. The focused browser harness now asserts inspector containment plus unclipped `Available`, `Needed for open repairs`, `Reorder at`, and `Incoming` labels and values across 1280/960/390/320. Focused components pass `33/33`; frontend production build, changed lint, Impeccable layout detector (`[]`), Playwright discovery (`3 tests in 1 file`), and diff integrity pass. The exact fresh Playwright execution and post-correction 1280 capture remain unverified because this sandbox again denied the configured `127.0.0.1:5181` bind with `listen EPERM`; this is not recorded as a product pass. QA remains held until Release adopts the new complete fingerprint from the handoff and independently executes the frozen browser gate and visual 1280 inspection.

DB-038 independent QA returned frozen candidate fingerprint `bda66392935040257b6409b445bc8f7d4faa67e76e5c9cb73b0c888c5584d8bb` NO-GO (`P0/P1/P2=0/1/0`) because the global new-presentation form rule forced the dark Parts & inventory `Catalog` and `Sort` selects onto a road-white background, leaving their selected values illegible. Frontend & UX corrected only those DB-038 toolbar selects: their background, foreground, border, and color-scheme now use the incumbent appearance tokens, with explicit system-color behavior under forced colors; the shared staff-form rule and every other workspace remain unchanged. The focused browser harness verifies the selected values plus computed text/background and border/background contrast in authored dark mode at 1280/390 and high-contrast mode at 960/320. Focused components pass `33/33`; frontend production build, changed lint, Impeccable detector (`[]`), and diff integrity pass. Release & Reliability independently executed the unchanged focused Playwright harness blob `0cb95bc7e9b5f5fab85d8bbb18e49f7d79a3fd70` and returned `3/3` in `17.3s`; visual inspection of `db038-toolbar-dark-1280.png` confirms both controls are cohesive with the dark workbench and their labels, selected values, borders, and dropdown indicators are legible. The implementation sandbox's separate `listen EPERM` on `127.0.0.1:5181` is retained as an environment limitation, not represented as product evidence. No product behavior, API, backend, migration, role, archive flow, global theme, runtime, flag, production, or customer-data change is included. QA remains held until Release freezes the corrected complete fingerprint and a fresh independent QA gate returns GO.

DB-038 Product P1 contrast/language recovery handoff: on unchanged parent `f992f5b2771625287caac9c244b8b32e2f511052`, Frontend & UX scoped token-driven editor colors, caret, placeholder, disabled/autofill, color-scheme, and forced-colors behavior to the active Parts workbench quantity/reorder and supplier-source controls without changing the global staff theme. Active Parts & inventory and Purchasing copy now uses `On hand`, `On-hand quantity`, `quantity`, and `units`; only genuine supplier metadata retains the explicit `Pack size` term, while backend `*_packages` fields remain unchanged. The exact localhost runtime identified itself as `codex/db038-ux-recovery@f992f5b`: live populated number and adjustment-textarea controls measured `17.93:1` contrast in Night shop at 1280 and 390 and `21:1` in High contrast at 1280, with zero rendered `packages`/`pkg` jargon in the checked workbench and Purchasing surfaces. Captures: `/private/tmp/db038-p1-dark-controls-1280.png`, `/private/tmp/db038-p1-dark-controls-editor-390.png`, and `/private/tmp/db038-p1-high-contrast-controls-1280.png`. Focused components pass `33/33`; production build, exact changed frontend lint, Impeccable detector (`[]`), and diff integrity pass. The updated frozen Playwright assertions cover authored dark/high-contrast and forced-colors behavior, but this sandbox's configured isolated server remains unable to bind `127.0.0.1:5181` (`listen EPERM`), so no new isolated Playwright pass is claimed. The earlier Release localhost schema-recovery evidence remains intact. The correction stays uncommitted and unpushed for visible Release adoption and fresh independent QA; no backend, API, migration, data, flag, deployment, or customer-data action occurred.

DB-038 Product visual-fidelity P1 recovery handoff: Product rejected the mechanically clean but visually flat Parts inspector because it did not meet the high-fidelity service-manual composition in approved option 5a. Frontend & UX retained every approved Parts, Purchasing, photo fallback, archive safety, role, movement, supplier-source, quantity-language, and scoped contrast behavior while rebuilding only the active Parts presentation: the ledger is the primary scan surface; the selected ledger row and selected-part action block share one bone-inversion token; the frame and inner controls use flat 5px/2px equipment geometry and structural rules; the inspector owns accessible Overview, Stock, Ordering, and History sections; oxide, bone-outline, and transparent actions have distinct primary, secondary, and tertiary roles; and stock, quantity, cost, SKU, PO, repair, and movement figures use the mono role. The focused browser harness asserts the shared selection surface, action hierarchy, local section ownership and keyboard behavior, contrast, structural geometry, mono figures, responsive containment, and authored dark/light/high-contrast/forced-colors behavior. Focused components pass `35/35`; the isolated DB-038 Playwright gate passes `3/3` in `17.3s` across 1280/960/390/320; production build, exact changed lint, diff integrity, and the final Impeccable layout detector pass. Current captures are `db038-service-manual-1280.png`, `db038-service-manual-960.png`, `db038-service-manual-390.png`, and `db038-service-manual-320.png` under the focused Playwright result directory. The candidate remains uncommitted and unpushed on parent `f992f5b2771625287caac9c244b8b32e2f511052`; Release must adopt the frozen identity and route a fresh independent QA gate before any delivery action.

DB-038 Release P2 compact-identity correction handoff: Release returned complete fingerprint `aa78d40e06c2de7b07886f4326470282690d199e0827c222001faf38d5014751` NO-GO (`P0/P1/P2=0/0/1`) because the DieselBridge lockup and development runtime badge overlapped at 320px in authored High contrast. Frontend & UX changed only the narrow staff-header composition: below 360px the existing product identity remains first in DOM/focus order, the DEV runtime identity reflows beneath it, and the staff viewport accounts for the taller header. The focused browser assertion now proves authored High contrast, non-intersecting product/runtime boxes, row and viewport containment, and unclipped visible labels at 320px. Focused components pass `35/35`; the isolated DB-038 Playwright gate passes `3/3` in `19.6s` across 1280/960/390/320; production build, exact changed frontend lint, Impeccable detector (`[]`), and diff integrity pass. Visual confirmation of the corrected 320px capture shows the complete `Powered by DieselBridge` identity and complete `branch@shortSHA` badge on separate rows with no overlap. The correction remains uncommitted and unpushed on parent `f992f5b2771625287caac9c244b8b32e2f511052`; visible Release must adopt the new frozen identity and route fresh independent QA before delivery action.

DB-038 option-5a Product rejection and Frontend recovery handoff: Product rejected the prior Parts workspace and bound the source-grounded option-5a composition plus the stock/purchasing clarification in `DB-038_PARTS_PURCHASING_REDESIGN_CONTRACT.md` section 3.1. Frontend & UX rebuilt the active surface as a dense service-manual ledger with independent checkboxes, line numbers, photos, SKU/bin identity, truthful Available/Needed/Reorder/Incoming/Average cost/Preferred supplier/Remarks columns, bone-linked selected row and inspector, Overview/Stock/Ordering/History sections, source-backed repair/supplier/receipt evidence, and owner/admin Add Part through the real `/inventory` capability. Needs reorder preselects only eligible rows on the loaded server page; selected assigned and unassigned rows are deduplicated into a tenant/user-scoped Purchasing preparation envelope; blocked unassigned rows remain visible and excluded from totals/batch submission; errors retain the tray and success removes only created lines. CSV, Free/Committed balances, unsupported bulk supplier/reorder mutations, and invented backend state remain absent. Focused components pass `42/42`; the production build, exact changed-file lint, diff integrity, and final Impeccable detector (`[]`) pass; Playwright discovery resolves the three focused journeys. The managed sandbox still denies the isolated Vite bind at `127.0.0.1:5181` with `listen EPERM`, so no new Playwright execution is claimed. The existing 5173 listener is mounted from this exact recovery worktree, but a fresh controllable browser has no authenticated shop session and correctly lands on staff login; no rendered Parts acceptance or screenshot is claimed from that state. Fresh Release/independent QA remain held. The recovery remains uncommitted and unpushed on parent `f992f5b2771625287caac9c244b8b32e2f511052`; no backend, API, migration, data, flag, service restart, runtime mutation, or delivery action is included.

DB-038 Release option-5a correction handoff: Release returned the frozen candidate NO-GO (`P0/P1/P2=0/3/0`) for an ambiguous Purchasing live-region assertion, a genuinely unreachable compact ledger row target, and 1280px workbench clipping. Frontend & UX retained both legitimate live regions and gave the purchase-preparation result an exact accessible name; the focused assertion now targets that notice rather than hiding either status. The Parts frame, toolbar, and master/detail owner now have explicit inline containment; the compact ledger owns a bounded scroll region, its row identity control stretches only within its grid track, and the compact header/toolbar reflow without creating a programmatically shifted hit target. The intentional Purchasing preparation component test remains in the frozen scope and passes with the rest of the focused components (`42/42`). Production build, exact frontend lint, diff integrity, and the final Impeccable detector (`[]`) pass. The frozen browser harness adds workbench/toolbar/inspector containment and zero-horizontal-scroll assertions, but the final isolated execution is not claimed: the managed sandbox denied the required untouched-5173-safe Vite bind on `127.0.0.1:5181` with `listen EPERM`. Visible Release must adopt the new exact diff/harness/test identity, run the three focused journeys on isolated 5181, inspect the 1280 and compact captures, and route fresh independent QA. The candidate remains uncommitted and unpushed; no shared listener, backend, API, migration, data, flag, production, or customer-data action occurred.

DB-038 Product Phase 1 Parts-ledger correction (Frontend & UX; Release and QA held): Parts now requests explicit server pages of 50 and appends only after `Load 50 more`, reports `Showing N of total`, and starts a fresh first page whenever search, view, stock filter, sort, or catalog context changes. The decorative ordinal column is removed. Owners/admins can independently select every active non-placeholder loaded part, including items without a reorder recommendation; Needs reorder preselects only eligible rows from each page as that page is loaded; archived/placeholder rows have no checkbox; receptionist/read-only sessions have no selection column or bulk controls. Compact mode keeps the ledger visible in its own bounded scroll region and separates checkbox and row-detail hit targets. Focused components pass `42/42`; production build, exact-file lint, Playwright discovery (`3 tests in 1 file`), Impeccable detector (`[]`), and diff integrity pass. The isolated browser run remains unverified because the mandated untouched-5173-safe Vite bind at `127.0.0.1:5181` was denied with `listen EPERM`; therefore no new 1280/compact captures or Playwright PASS are claimed, and Product inspection remains blocked pending visible Release execution on isolated 5181. Phase 2 was not started. The candidate remains uncommitted and unpushed; no shared listener, backend, API, migration, data, flag, production, or customer-data action occurred.

DB-038 open Product-observation batch (Frontend & UX; Release and QA still held): the Parts controls now belong only to the ledger, with a compact search, All/Below min/Out of stock plus Archived parts, and keyboard-accessible Sort/Density inside one Options popover. The Selected Part workspace receives the larger secondary column. Ledger rows have no horizontal separators and retain restrained vertical alignment guides. The ledger presents Description, contract-backed Available, truthful Bin location (`Bin not set` fallback), Average cost, Preferred supplier, and Remarks; Needed for open repairs, Reorder at, and Incoming are removed from the ledger and remain only in the selected record's Overview/Stock context. A superseding Product correction retains the complete seven-track owner/admin ledger and six-track read-only ledger: header and row share the exact computed template, Description aligns with the name/SKU text after the photo, numeric headings align right, content headings align left, and every heading uses single-line normal wrapping with no forced word break. Compact layouts preserve Description/Available/Bin priority without page overflow. `Showing N of total` and `Load 50 more` remain ordinary non-sticky natural-bottom ledger content. Focused components pass `42/42`; isolated Playwright passes `3/3` in `25.1s` across 1280/960/390/320 with exact column order/presence, only Needed/Reorder/Incoming absent, header/row grid parity, Description-to-identity alignment, text-fit, natural-bottom, line-treatment, toolbar ownership, compact hit-target, and unchanged workflow assertions. Production build, exact focused frontend lint, diff-check, and the single final Impeccable layout detector (`[]`) pass. Current 1280/390/320 and natural-bottom captures are in the focused result directory. The Product observation pass remains open: this state is intentionally uncommitted/unpushed and must not be frozen or routed to Release/QA until Product explicitly closes observations. Shared 5173, backend, API, migration, data, flags, production, and customer data remain untouched.

DB-038 continuing Product-observation evidence (Frontend & UX; Release and QA still held): the selected part's `Supplied by` relationship now uses one flat frame with source-first lead time, real supplier delivery reliability, an explicit unavailable-history fallback, single separators, and a point-of-use `Change` action. The ledger's first Available guide is controlled by the named `parts-ledger` container: it is absent above the verified 50rem scan threshold and restored below it; Bin/Average cost and Preferred supplier/Remarks guides remain in the header while those two body-row strokes are intentionally absent, with all other approved guides and seven/six-track parity retained. Stock facts now use single 1px structural rules, clear every even-column outer-right stroke, and collapse without vertical rules in a narrow inspector. Both Available and Reorder at reuse the shared 44px `QuantityStepper`; the selected-part shortcut and Stock-tab Available action converge on one editor, Available still requires the audited reason, and Needed/Incoming remain read-only. Focused components pass `45/45`; isolated Playwright passes `3/3` in `27.6s` across 1280/960/390/320; exact frontend lint, TypeScript/production build, responsive geometry, themes, focus, exact payloads, and fresh desktop/compact captures pass. Product separately reproduced a `Needs reorder` summary badge of 695 against a selected-view result of 5/5; that display is held for visible Architecture because it is an API summary/query count-contract mismatch, and no UI placeholder, height, pagination, or count workaround was added. The observation pass remains open and the candidate remains intentionally uncommitted/unpushed; shared 5173 and all backend/API/data/migration/flag/runtime/release surfaces remain untouched.

DB-038 continuing Product-observation evidence II (Frontend & UX; Release and QA still held): Frontend now consumes the Architecture-approved `needs_reorder_count` summary with the rollout-only `low_stock_count` fallback, uses `N NEEDS REORDER` and `Needs reorder` consistently, and treats the selected reorder collection total as immediate view authority. The app-owned Sort/Options control replaces the native select, aligns Sort and Density, closes on choice/outside/Escape with focus restoration, retains accessible keyboard navigation, and preserves eligible checked IDs across all four sort modes without changing the current view/filter. Purchasing preparation now replaces the native number field with the shared 44px `QuantityStepper`; its part-specific accessible name, pack/minimum normalization, typed-value normalization, supplier total, batch payload, removal, blocked lines, and dark/light/high-contrast/forced-colors treatment are covered, and its desktop/compact grid prevents stepper/cost overlap. Focused components pass `48/48`; isolated Playwright passes `3/3` in `28.6s` across 1280/960/390/320 with summary reconciliation, selection persistence, custom Options semantics, computed contrast/focus, stepper geometry, total and payload evidence. Production build, exact frontend lint, and diff integrity pass; current captures include `db038-ledger-sort-options-1280.png`, `db038-purchase-quantity-1280.png`, and `db038-purchase-quantity-390.png` under the focused result directory. Backend's independently owned correction remains preserved (`bff7149c16afce46cdefb7f4456b6f51ea6a874c76cd2fc6e6274b62d4f61bf1`, focused `17/17`): summary returns the new count with an identical legacy alias and the actionable filter excludes placeholder/archived/deleted/foreign records. The shared API was not restarted, so current port 8000 is not adoption evidence. Shared 5173 remained PID 34533 at the same candidate frontend cwd before/after every isolated 5181 run; HTTP probing remains sandbox-inaccessible (`000`) while the listener identity stayed unchanged. The observation pass remains open; this state is intentionally uncommitted/unpushed and is not frozen or routed to Release/QA.

DB-038 continuing Product-observation evidence III (Frontend & UX; Release and QA still held): the Parts stock editor now distinguishes pointer from keyboard activation while preserving autofocus inside the newly revealed inline editor. Pointer/touch opening produces no inner-input copper ring, double border, or detached halo; keyboard opening and subsequent keyboard navigation produce one restrained focus outline on the complete shared quantity stepper, with the inner field and buttons explicitly ring-free. Cancel/save retain the existing invoker-focus restoration, and authored high contrast plus forced colors use system focus colors. Both Adjust on-hand quantity and Change reorder point use the same state and styling; the shared Purchasing stepper is unchanged. Focused Parts/legacy/Purchasing components pass `49/49`; Playwright discovery remains `3` tests in one file, production build, focused frontend lint, and diff integrity pass. The correction's isolated 1280/960/390/320 Playwright rerun and new captures are not claimed: Vite was denied binding `127.0.0.1:5181` with `listen EPERM` before the app started. Shared 5173 remained protected and was not used as substitute evidence. The observation pass remains open; this state is intentionally uncommitted/unpushed and is not frozen or routed to Release/QA.

DB-038 continuing Product-observation evidence IV (Frontend & UX; Release and QA still held): the Stock facts now edit progressively at the point of use. The permanent `Adjust available` / `Change reorder point` row and detached editor card are removed; active owner/admin records instead expose restrained 44px edit controls beside Available and Reorder at, with exact accessible labels and contained tooltips. Activating either replaces only that fact value with the shared `QuantityStepper` plus compact Save/Cancel; only Available reveals the audited Adjustment reason, only one draft can exist, the selected-part shortcut converges on the same editor, API failure retains the draft/error, and cancel/save restore the invoking control. Receptionist/read-only and archived records expose no mutation affordance, while Needed for open repairs and Incoming remain plain read projections. Focused Parts/legacy/Purchasing components pass `50/50`; the isolated 5181 Playwright gate passes `3/3` in `30.2s` across 1280/960/390/320 after two stale inline-DOM harness locators were corrected, with reason/payload/failure, same-editor, pointer/keyboard focus, tooltip containment, responsive geometry, theme, read-only, archive, and focus-restoration evidence. Production build, exact frontend lint, diff-check, and Impeccable detector (`[]`) pass; current Stock captures are `db038-stock-tab-1280.png` and `db038-stock-tab-390.png` under the focused result directory. Shared 5173 remained PID 34533 in the same candidate frontend cwd before/after; its already-recorded sandbox-inaccessible HTTP probe remained `000`, and it was not restarted or reused. The observation pass remains open; this state is intentionally uncommitted/unpushed and is not frozen or routed to Release/QA.

DB-038 continuing Product-observation evidence V (Frontend & UX; Release and QA still held): compact Stock editors now keep the shared stepper's minus, numeric value, plus, and visible `units` label on one aligned line at 390px and 320px. The Parts-scoped unit lane follows the established Purchasing treatment, cannot shrink or break between letters, and retains the full accessible unit text. The rendered inspection also found and removed a second keyboard-focus outline from the unit label: the pill is now the sole restrained focus owner, while pointer-open state has neither a focus halo nor an always-visible tooltip. Focused Parts/legacy/Purchasing components pass `50/50`; the isolated 5181 Playwright gate passes `3/3` in `31.0s` across 1280/960/390/320 with computed no-wrap/fit/alignment, 44px targets, single-focus-owner, neutral-pointer, containment, and existing behavior/payload assertions. Production build, exact frontend lint, diff integrity, and the single final Impeccable detector (`[]`) pass. Fresh held captures are `db038-stock-stepper-pointer-390.png`, `db038-stock-stepper-keyboard-390.png`, `db038-stock-stepper-pointer-320.png`, and `db038-stock-stepper-keyboard-320.png` under the focused result directory. Shared 5173 remained PID 34533 in the same candidate frontend cwd with the same sandbox-inaccessible `000` HTTP probe before/after and was not restarted or reused. The observation pass remains open; this state stays intentionally uncommitted/unpushed and is not frozen or routed to Release/QA.

DB-038 continuing Product-observation evidence VI (Frontend & UX; Release and QA still held): Ordering supplier-source rows now retain their flat relationship layout while active owner/admin rows expose one persistent trailing `Edit source` cue with a Pencil icon and an exact supplier-specific accessible name. Opening an existing or new source records pointer-versus-keyboard modality locally: pointer/touch autofocus is visually neutral with the normal token border and caret, while keyboard activation and later Tab navigation show one restrained technical focus outline on the actual field or control; authored High contrast and forced colors use system focus colors. Cancel and successful close restore focus to the exact source or Add-source invoker. While one editor is open, other source rows become truthful static content with no misleading action cue; receptionist and archived records are also noninteractive rather than disabled fake buttons. The first isolated 5181 browser run returned `2/3` and reproduced one exact 320px authored-High-contrast defect: the more-specific shell focus rule restored a 2px pointer-open outline. A Parts/Supplier-only specificity correction clears that pointer state without changing keyboard or system focus; the unchanged three-test harness then passes `3/3` in `32.1s` across 1280/960/390/320. Focused Parts/legacy/Purchasing components remain `52/52`; production build, exact focused frontend lint, diff integrity, and Impeccable detector (`[]`) pass. Visual inspection confirms the flat row cue, metadata, and cost stay legible and contained and the pointer editor stays neutral in `db038-ordering-source-row-1280.png`, `db038-ordering-source-editor-pointer-1280.png`, `db038-ordering-source-row-390.png`, and `db038-ordering-source-editor-pointer-390.png` under the focused result directory. Supervisor preflight independently returned HTTP 200 for protected 5173; this managed execution context's before/after curl remained namespace-inaccessible at `000`, while both checks retained PID 34533, the exact candidate frontend cwd, and the running `com.dieselbridge.db038-vite` launchd job. Isolated 5181 exited after the test and shared 5173 was not restarted, reused, or mutated. The observation pass remains open; this state remains intentionally uncommitted/unpushed and is not frozen or routed to Release/QA.

DB-038 continuing Product-observation evidence VII (Frontend & UX; Release and QA still held): the active Available or Reorder-at editor now owns the complete two-column Stock-facts row at every inspector size, while Needed for open repairs and Incoming remain truthful read-only facts below it; closing restores the ordinary 2x2 facts composition. The flat inline editor retains the shared QuantityStepper and audited Available reason, replaces the wrapping Cancel/`Save change` pair with one non-wrapping `Save` action and a separate 44px lucide-X cancel control at the fact's top trailing edge, supports Escape from anywhere in the editor, disables both commands while saving, and restores focus to the exact shortcut or fact invoker after Save, X, or Escape. Pointer-open focus remains neutral, keyboard focus remains owned by the whole stepper, and the X tooltip is contained and hidden in the neutral state. Focused Parts/legacy/Purchasing components pass `52/52`; production TypeScript/build, exact Parts lint, diff integrity, Playwright discovery (`3 tests in 1 file`), and the Impeccable layout detector (`[]`) pass. Frontend's managed sandbox denied the isolated Vite bind at `127.0.0.1:5181`, so it correctly claimed no rendered acceptance; the supervisor then executed the unchanged focused harness from an authorized isolated context and returned `3/3` in `34.6s` across 1280/960/390/320. Visual inspection of the fresh Available/Reorder captures confirms full-row ownership, read-only-fact placement, 44px X, single-line Save, contained reason/stepper geometry, neutral pointer focus, authored High contrast, and no horizontal overflow. Protected 5173 remained PID 34533 at the exact candidate frontend cwd and HTTP `200` before and after; isolated 5181 exited and shared 5173 was not restarted, reused, or mutated. The Product observation pass remains open; this state stays intentionally uncommitted/unpushed and is not frozen or routed to Release/QA.

DB-038 compact runtime-identity QA correction (Frontend & UX; QA remains held): independent QA returned exact commit `cb85f9bb99638b66005047b938d618f909dfb340` NO-GO (`P0/P1/P2=0/0/1`) because the development `branch@shortSHA` badge was centered wider than the 84px collapsed rail at 960px, leaving only a clipped mid-string fragment visible in both light and forced-colors captures. The bounded correction deliberately suppresses that development-only badge only while the desktop rail is collapsed; expanded desktop and the existing 390/320 mobile identity compositions remain unchanged. The focused browser harness now requires the 960 badge to have `display:none`, zero client rects and zero width, plus a contained brand row/body, while retaining visible contained identity checks at 1280/390/320 and dedicated 960 light/forced-colors captures. Focused shell/runtime components pass `11/11`; production build, changed frontend lint, Playwright discovery (`3 tests in 1 file`), diff integrity, and the Impeccable detector (`[]`) pass. This execution sandbox denied both isolated 5181 attempts with `listen EPERM` before the app started, so no post-correction browser PASS or captures are claimed here. Shared 5173 remained read-only and was not used as substitute evidence. The correction remains intentionally uncommitted/unpushed for Product to freeze and route to a fresh independent QA rerun in an authorized isolated browser context.

DB-035 navigation polish addendum: under the new presentation only, Settings now uses one compact, labelled mobile section selector rather than a horizontally scrolling tab strip; all existing role/permission section gates remain intact. The authenticated mobile shell moves its active copper marker to the bottom edge. Focused Settings and shell tests pass `9/9`; changed lint, production build, Impeccable detector, and diff integrity pass. Live authenticated browser review remains pending because the controllable browser session is at `/login`; no auth session was changed.

DB-035 connected-workspace correction (Frontend & UX Recovery Owner, Product review pending): the new presentation now keeps Shop Work strictly as the server-backed action queue and opens the selected canonical repair order directly in Repair Orders. Repair Orders renders that selection as a persistent master/detail workspace at desktop widths; at compact/mobile widths it becomes a canonical list-to-record transition with the same selected URL and queue return context. The existing protected Sidekick remains unchanged for legacy and other incumbent uses. No Dashboard detail API, history/payment/authorization duplication, new route, mutation, or backend contract was added. Focused Repair Orders tests pass `17/17`; production build and Impeccable layout detector pass; direct changed-file lint has no errors (one existing exhaustive-deps warning). Authenticated browser visual acceptance is still pending.

Fleet navigation ownership follow-up: the duplicate Fleet entry is removed from My Shop while the existing Fleet configuration/overview remains available from Settings under **Additional services**, anchored at the bottom of the full-height desktop Settings rail. The existing `/fleet` board handoff remains unchanged. Focused Settings tests pass `4/4`, production build and Impeccable detector pass, and the direct My Shop source contains no Fleet navigation entry. Live authenticated browser verification remains pending at `/login`; no authentication state was changed.

Fleet return-context follow-up: opening the existing Fleet board from Settings now carries `/dashboard/settings?section=fleet`; the Fleet rail returns to that exact prior Settings entry, while a direct `/fleet` visit keeps its existing Shop Work fallback. Focused Settings/Fleet return tests pass `6/6`; production build passes. Live authenticated browser review remains pending at `/login`; no authentication state was changed.

Shell identity follow-up: the new-presentation rail now leads with a larger centered tenant identity, then a shared metadata row of the tenant state and a quieter `Powered by DieselBridge` endorsement rather than three equal brand elements. The existing landing-derived DieselBridge wordmark animation remains; the former standalone separator rule is removed. Focused shell/Settings regression passes `10/10`; live authenticated visual review remains pending at `/login` with no authentication state changed.

Responsive shell identity motion follow-up: the horizontal compact shell reveals tenant identity first, then state, then the `Powered by DieselBridge` endorsement and its existing wordmark motion in one row. The expanded desktop rail pairs a larger tenant logo with the state and places the staged platform endorsement beneath it; the collapsed rail keeps only tenant logo and state. The broad anchor border that created a spurious separator under the endorsement is removed. Reduced-motion preference receives the settled identity without the new staged sequence. Focused shell tests pass `6/6`; production build, Impeccable detector, and diff integrity pass. Live authenticated visual review remains pending at `/login`; no authentication state was changed.

Fleet configuration density follow-up: the locked Fleet Configuration surface now presents its existing Fleet Company and Default Operating Authority values as one compact two-column summary, followed by the unchanged password-gated unlock action. Fleet Overview now uses internal ledger dividers rather than nested rounded manager/truck cards. Existing fields, password verification, save behavior, and Fleet Board handoff are unchanged. Focused Settings tests pass `4/4`; production build, Impeccable detector, and diff integrity pass. Live authenticated visual review remains pending at `/login`; no authentication state was changed.

Fleet overview hierarchy follow-up: Fleet managers now use one compact identity row, while fleet vehicles are presented as a semantic Unit / Vehicle / Status table with a single header boundary and quiet row separators. The Settings rail no longer repeats underline rules for Account and Shop; its only rail boundary identifies the optional Additional services capability. Existing Fleet data, permissions, settings route, and Fleet Board handoff are unchanged. Focused Settings tests pass `4/4`; production build, Impeccable detector, and diff integrity pass. Live authenticated visual review remains pending at `/login`; no authentication state was changed.

Repair Orders responsive filter follow-up: new presentation only. At tablet widths the existing search control occupies its own row and all real status filters wrap into a reachable touch-friendly quick-filter row rather than clipping off-canvas. At compact widths the existing status select remains the authoritative control but aligns to the 44px search baseline, carries a custom chevron affordance, and uses an external focus treatment. Existing status values, callbacks, query parameters, selected-order behavior, routes, permissions, APIs, and mutations are unchanged. Focused Repair Orders tests pass `8/8`; production build, changed-file lint, and Impeccable detector pass. A live authenticated desktop DOM check found all nine real filters and zero console warnings/errors; the controlled in-app browser did not honor its temporary viewport override, so it is not offered as medium/mobile visual evidence.

Repair Orders detail hierarchy follow-up: new presentation only. The presentation-only `Selected repair order` card is removed because it repeated identity and summary facts already owned by the selected repair-order workspace. The existing order header remains the sole display of order type, number, status, customer, and vehicle; requested work, labor/parts, history, and financial context remain in their existing real sections. When the order was opened from a canonical Shop Work queue, the queue-origin return remains a compact header navigation action rather than a second data surface. Legacy rendering, URL/history semantics, hooks, APIs, permissions, mutations, and selected-order behavior are unchanged. Focused Repair Orders tests pass `17/17`; production build and live authenticated DOM inspection confirm no detached context card is rendered.

Repair Orders selected-workspace contrast/frame follow-up: new presentation only. The existing navy header now explicitly owns a road-white order identifier, preserving readable identity against its product field. The Price Builder no longer renders as a rounded, bordered card nested inside the selected-order drawer: the actual Sidekick drawer owns the sole boundary and the workspace fills it directly. Existing selected-order data, workflow controls, disclosures, routes, permissions, APIs, mutations, legacy presentation, and return-context behavior are unchanged. Focused Repair Orders tests pass `17/17`; production build, changed-file lint, diff integrity, and live authenticated DOM inspection pass.

Repair Orders toolbar follow-up: new presentation only. The toolbar now keeps a 3px internal focus runway so the search treatment stays inside the scroll owner instead of being clipped at its left edge. It also uses the actual Repair Orders canvas as its responsive container: when a Sidekick, browser zoom, or medium-width canvas is constrained, search occupies its own row and all nine existing quick filters wrap into a reachable row. The compact native select remains unchanged below the existing mobile breakpoint. Existing filter values/callbacks, selected-order behavior, URLs, permissions, APIs, mutations, and legacy presentation are unchanged. Focused Repair Orders tests pass `17/17`; production build, changed-file lint, and diff integrity pass. Authenticated browser inspection confirms the hot update, internal search runway, nine real filters, and no horizontal page overflow; independent QA/Product review remains required.

Shop Work direct-handoff correction: new presentation only. A real queue-row activation now opens the exact canonical Repair Orders URL with its selected repair-order ID and canonical lane in one click or keyboard activation; Dashboard no longer renders a read-only pseudo-detail strip or a second "Open" action below the queue. The Action Ledger remains limited to its existing server-backed projection and the existing Repair Orders workspace remains the sole owner of sidekick detail, history, authorizations, invoices, payments, and mutations. Focused Dashboard/Action Ledger tests pass `9/9`; changed-file lint, production build, and diff integrity pass. The standalone browser runner has no authenticated session and lands at `/login`, so live authenticated Product review remains required.

Repair Orders continuous-workspace correction: new presentation only. Shop Work queue selection lands directly on the existing canonical `?selected={id}&queue={lane}` Repair Orders workspace. Repair Orders no longer repeats that context in a handoff banner, an `All orders` control, or a secondary return action: the single Shop Work breadcrumb carries the lane context back to the queue, including when Dashboard remains mounted. The compact ledger scope control exposes `All repair orders` as the deliberate way to clear only `queue` while retaining `selected`. Pointer selection remains in the ledger; Enter or Space moves focus to the named inline workspace region. Legacy Sidekick behavior, canonical routes, IDs, query/history semantics, queue projection ownership, existing hooks/APIs/mutations, roles, permissions, and WebSocket invalidation are unchanged. Focused component evidence is `43/43`; production build and diff integrity pass; changed-file lint has no errors and one existing exhaustive-deps warning. The one-run Impeccable detector surfaced inherited Price Builder token warnings outside the two changed Price Builder hunks; they are not accepted as this workspace correction’s visual approval. Product review remains in progress. Independent QA/Security and release remain unauthorized.

Repair Orders keyboard-arrival refinement: new presentation only. A keyboard activation from the canonical order ledger now lands on the selected repair order’s own heading, with a compact rounded focus treatment on that heading rather than a copper frame surrounding the entire master/detail workspace; pointer selection remains in the ledger. The workspace stays a labelled inline region, its query/route/queue semantics, real repair controls, legacy Sidekick behavior, permissions, APIs, mutations, and WebSocket invalidation are unchanged. Focused Stage 4 tests pass `27/27`; changed TypeScript lint, production build, diff integrity, and the one-run Impeccable detector pass. The live recovery stack remains the existing recovery-worktree frontend and `truckpitstop_db035` backend; Product visual/interaction review remains required. Independent QA/Security and release remain unauthorized.

Repair Orders master/detail navigator consolidation: new presentation only. When a canonical repair order is open, the left pane condenses into a navigator—compact title/create action, search, and the existing authoritative status selector—rather than repeating a second filter dashboard. The result list now scrolls independently, is keyboard reachable as the labelled `Scrollable repair-order results` region, and keeps only scan-level order number, status, work, and amount; customer and vehicle identity remain solely in the existing selected repair-order workspace. Selected-row treatment is an inset copper cue rather than a clipped boxed outline. Existing query/history/queue semantics, routes, IDs, real workspace controls, legacy Sidekick behavior, permissions, APIs, mutations, and WebSocket invalidation are unchanged. Focused tests pass `21/21`; changed-file lint reports no new violations, production build, diff integrity, and the Impeccable detector pass. The recovery frontend and `truckpitstop_db035` backend are running, but the controlled browser currently has only an unauthenticated `/login` session, so this correction still requires live authenticated Product review. Independent QA/Security and release remain unauthorized.

### DB-035 backend implementation handoff

Backend implementation is complete on isolated branch `codex/db035-authenticated-presentation`, based on Architecture commit `5cf2e1f41b6f1180ba4d5846fc6323dabc9b9785`. It adds the additive tenant default and dedicated user/tenant appearance and rollout records, shared legacy/WorkOS authenticated bootstrap resolution, staff appearance GET/PUT/reset APIs, super-admin tenant/user rollout APIs, strict curated-value validation, tenant-scoped optimistic revision writes, global-force-legacy precedence, and redacted presentation metrics. Focused DB-035/auth/WorkOS tests pass `41/41`; focused backend/security/auth regression set passes `177/177`; PostgreSQL 15 empty migration reaches sole head `118_authenticated_presentation` and schema preflight passes; complete pinned Python 3.11 backend passes `744 passed, 4 skipped`; syntax compilation, diff check, and OpenAPI import/route checks pass. One WorkOS bootstrap compatibility regression found during the full suite was fixed by resolving presentation against the validated WorkOS principal tenant when the local user row has no tenant. Frontend changes remain unstaged and are handed off to Frontend & UX against Contract v1. Independent Security and QA gates remain required; no push, PR, merge, deploy, or self-approval is claimed.

### DB-035 frontend Harden handoff

Product approved the source-grounded Shape and authorized only Harden. The local candidate on `codex/db035-authenticated-presentation`, layered after backend `f314ab9`, removes the rejected local selected-order Dashboard model and associated fake hierarchy while leaving `DashboardHome` on its canonical action-queue projection, queue/activity switch, quick/full order actions, refresh behavior, statuses and repair-order deep links. The presentation resolver, identity-scoped cache, legacy-key migration, reversible Appearance editor, semantic/personal token separation, notification safety, DieselBridge-primary new shell and legacy presentation remain intact. Responsive shell ownership is bounded to a full 1280+ rail, compact 960–1279 rail, and the existing mobile navigation below 960. Focused tests pass `13/13`; full frontend passes `186/186`; production build, direct changed-source lint, and diff integrity pass; DB-035 Playwright passes `6/6`. This is an intermediate Harden milestone: the approved six-surface structural recomposition, Adapt, Optimize and Polish are not claimed. No push, PR, merge, deploy, Security approval, QA approval, Impeccable/Emil finish approval, or self-approval exists.

### DB-035 controlled recovery

Product rejected local Adapt commit `5c8c698` as a product outcome: it preserved
behavior but substituted a rail/queue rearrangement for the connected operating
experience promised publicly. It is audit evidence only and is not eligible for
QA, integration or release. Recovery restarts from accepted Harden `a7b7fc0` on
`codex/db035-ux-recovery`. The replacement owner produced two isolated,
source-grounded prototype directions outside production source. Product now owns
the single topology decision recorded in `docs/DB-035_MASTER_EXECUTION_PLAN.md`;
no production implementation resumes before that decision.

## Blocked

| ID | Priority | State | Outcome | Owner | Blocker | Resolver / next safe action | Unblock evidence |
|---|---|---|---|---|---|---|---|
| DB-006 | P0 | Blocked | Prove driver portal custody-only equipment isolation | QA Gatekeeper | Requires an authenticated WorkOS driver session for the exact linked driver; prior manager/invitation checks passed but driver isolation remained NO-GO | Product owner completes exact linked-driver MFA; QA then runs read-only isolation checks | Positive current-custody view plus denial of released, unrelated, fleet-wide, and cross-tenant equipment |
| DB-007 | P1 | Blocked | Enable Google Business Profile review integration | Backend & Integrations | Google Basic API Access case `1-9174000041216` remained in progress with quota 0 in the latest task evidence | Google approves the existing case; Backend rechecks quota without resubmitting | Vendor approval and non-zero quota, then location-selection and tenant-isolation acceptance |
| DB-008 | P1 | Blocked | Complete QuickBooks production readiness | Product & Delivery Lead | External Intuit questionnaire/production approval and test-account steps are not conclusively closed in task evidence | Product owner/Intuit completes approval or credential step; Backend performs sandbox-to-production verification | Approved credentials, documented environment setup, sandbox-to-production acceptance |

## Ready / backlog

| ID | Priority | State | Outcome | Owner | Acceptance target |
|---|---|---|---|---|---|
| DB-010 | P1 | Ready | Add Sentry error monitoring | Release & Reliability | Backend/frontend SDKs configured without secrets in git; releases/environment tags; source maps; alert ownership; verified test event and privacy filtering |
| DB-011 | P2 | Ready | Connect the board to Linear | Product & Delivery Lead | Connected workspace/team, statuses matching this board, ownership/priority/acceptance fields, and import without duplicate issues |
| DB-013 | P1 | Ready | Create an isolated staging environment | Release & Reliability | Production-like configuration with isolated data; migrations, acceptance suite, rollback rehearsal, and promotion record |
| DB-014 | P1 | Ready | Fix local seed idempotency | Backend & Integrations | Seed works with existing multi-tenant data without `MultipleResultsFound`, does not duplicate records, and supports a clean first run |
| DB-016 | P1 | Ready | Add repair-order concurrency and audit hardening | Architecture & API Contracts | `lock_version` conflict behavior, before/after audit for financial/workflow changes, user-visible history, and simultaneous-edit tests |
| DB-017 | P0 | Ready | Verify canonical work-first repair workflow end to end | QA Gatekeeper | Execute `FLOW_VERIFICATION.md`, including internal fleet, payment methods, void/revise, worker/outbox email, and immutable invoice behavior |
| DB-020 | P1 | Ready | Fix production apex-domain availability | Release & Reliability | Verify DNS/TLS/redirect ownership and make apex reliably redirect or serve without affecting `www` |
| DB-022 | P0 | Ready | Expand Playwright from smoke coverage into the product regression safety net | QA Gatekeeper | Add repair order, estimate authorization, payment, customer portal, driver custody, and cross-tenant denial journeys; require them in CI only after stable repeated runs |
| DB-023 | P1 | Ready | Restore a clean repository-wide frontend lint baseline | Frontend & UX | Resolve the recorded 175 errors and 27 warnings without behavior regressions, then make repository-wide lint blocking |
| DB-024 | P0 | Ready | Remediate frontend dependency risk | Security & Identity | Triage the recorded npm audit baseline (1 low, 5 moderate, 26 high, 4 critical), upgrade safely, produce SBOM/audit evidence, and keep build plus browser journeys green |
| DB-025 | P1 | Ready | Add infrastructure-level outbound webhook egress defense | Release & Reliability | Route conversion webhooks through an egress control that denies loopback, private, link-local, metadata, and ULA destinations; Railway currently provides outbound networking/static IP features but no configured per-service private-address deny. Keep the application DNS pinning and URL controls as the primary guard until then |
| DB-026 | P1 | Ready | Run the Celery worker as a non-root user | Release & Reliability | Container starts under a dedicated unprivileged UID, worker/beat tasks remain healthy, filesystem permissions are explicit, and the Celery root warning disappears |

## Completed with evidence to retain

These outcomes retain their implementation, review, CI, and release evidence.
Rows that lack an explicit production record remain implementation evidence only.

| Outcome | Evidence |
|---|---|
| DB-031 — Deterministic exception error persistence | PR #261 merged as `f93ed6b87c5d8fc654474e81bf7a8ee309936bef`; exact application candidate `212a9bfcb0c855323ef95f9efb2a799a1a62eaf5` received Architecture, constitutional fallback Security, dedicated QA, and Release fallback QA GO with no P0–P2. Security passed `264/264` simultaneous resilience plus secret checks. QA passed twenty fresh Python 3.11 lifecycle/context processes (`660/660`), QuickBooks `5/5`, combined focused `122/122`, two full backend runs at `741 passed, 4 skipped` each, clean teardown/lifecycle scans, preserved strict `len(sessions) == 2`, compile/diff integrity, and sole Alembic head `117_conversion_export_security`. All six protected CI contexts passed. Railway production web `e625da0a-a307-4c2b-9792-8e8d7222c001`, performance web `7bd587b5-e62f-47ac-a9a0-bf69d67255cc`, and worker `5e7ab03c-46f4-48bd-86fa-89c967161c2d` succeeded on the merge SHA; both web predeploys ran Alembic transactionally and passed `/health`. Controlled production canary `db031-canary-f93ed6b8-20260811t1954z` returned a sanitized 500 and persisted exactly one sanitized error row with the synthetic secret marker absent. QuickBooks configuration remained valid (Accounting sandbox, Payments production), Stripe remained configured, and both status endpoints remained auth-protected. Observation `2026-08-11T19:47:50Z–19:59:36Z` had repeated production/performance 200s, zero Railway HTTP 5xx, unchanged error metrics, successful scheduled worker tasks, and zero pending-task, `GeneratorExit`, `IllegalStateChangeError`, unraisable, session-overlap, cancellation-failure, traceback/task-failure, or critical-worker markers. Rollback target `42085d527b449d3ac2f461c4e1c0f909171088fe` and prior healthy deployments production `d21e7b67-3688-4284-b38a-4397833e7cfe`, performance `f015e742-5658-4ec5-8bdf-567e81712aeb`, worker `5c73e3f2-02e0-462a-9597-334ccf019455` remain recorded; no rollback trigger fired. No migration, dependency, frontend, API-contract, auth, tenant, or payment behavior change. |
| DB-030 — Customer portal touch-target repair | PR #257 merged as `7797e608f2cf0bf89486ea8b8a49864d2df2a9f6`; all six protected CI contexts passed. Candidate `a2ab564b834513e82ef5bfb60afebadfc3d2af98` received independent QA GO: the four reported controls measured at least 44×44 at 390px and 320px, focused portal/title/route tests passed 16/16, full frontend passed 145/145, production build, changed-file lint and diff checks passed, and the mobile Playwright journey passed 2/2 with containment, keyboard, payment/PDF behavior, and no browser errors. Railway production deployment `a788d9f3-93ee-4781-90ee-e24fa5c02084` and performance deployment `a69e962e-10d7-4268-8c62-c3d3f2124f44` succeeded on the merge SHA and passed `/health`; worker deployment `2066ffd9-8d61-4326-8c1b-e03833cd99e3` remained unchanged. The deployed non-mutating safe-fixture portal journey passed 2/2 at 390px and 320px in 5.0 seconds. Five initial probes plus 20 observation rounds over ten minutes returned HTTP 200 for the homepage, production API health, and performance health, with no Railway HTTP 5xx entries. Rollback target was `4c2362cede901ad4768a9685b89a5f2abe00919e`; no rollback trigger fired. No backend, API, auth, tenant, payment behavior, dependency, worker, data, or migration change. |
| Mobile repair-order scrolling and finalized-photo behavior | PR #195, commit `222ba23`, focused mobile/runtime checks reported |
| Mobile platform performance layout | PR #161, commit `7789b0e`, 320px no-overflow/build evidence reported |
| Customer portal visual redesign | PR #189, commit `d9daf91`, build and portal/logo tests reported |
| Customer portal active repairs/action-required restoration | PR #194, commit `37e5e9a`, build and targeted tests reported |
| Local Docker database alignment | Commit `12dbbe1`, `truckpitstop` database configuration |
| WorkOS identity and authorization rollout | PRs #225–#240 are merged and retained in production SHA `2ff3910b`; authorization regression coverage passed the repaired full-suite and every later required release gate; no duplicate stale-branch reship is required |
| Fleet performance optimization | PR #193 is merged and included in current production; no PR retry is required |
| DB-HC001 — House Cleanup and delivery-team governance | PR #241; all intentional dirty paths classified; source work preserved without deleting local backups/tooling; focused follow-up releases #242–#246; final clean workspace branch created from current `main` |
| DB-018 — Green full-suite baseline | PRs #242 and #243; frontend 117/117 and backend 568/568 passed before later DB-002 expansion; the final DB-002 CI run also passed every required and informational check |
| DB-009 — Playwright CI smoke foundation | PR #244; PostgreSQL and Redis-backed CI starts the real services and passes 5/5 staff-login/public-invoice smoke tests; broader product journeys are DB-022 |
| DB-012 / DB-019 — Protected delivery workflow | `main` requires strict migration, frontend, backend, full-suite, and Playwright contexts; administrators are included; force push/deletion are blocked; linear history and resolved conversations are required |
| DB-021 — Railway health-gated cutover | PR #245; production API deployment `ac540450-8f13-4fa6-b7ba-6461e75b9526` and performance deployment `c83d792f-68d5-4d5e-9660-8d0357bea7a4` passed `/health` with a 300-second gate at `2ff3910b`; repeated probes had no connection-refused/5xx responses |
| DB-028 — Rich homepage repair-order proof | PR #251 merged as `c65af774`; all six required and informational CI contexts passed. Independent QA and Impeccable finish re-reviews returned GO with no P0–P2 findings after target-size, financial arithmetic, first-viewport, glass-edge, motion, responsive-contract, and active-card geometry remediations. Railway production deployment `3c80c935-df22-4796-aa7e-a53cd0b6278c` and performance deployment `a9bda4f1-5677-49f7-a7f7-ab589dda81bd` succeeded on the merge SHA while worker deployment `2066ffd9-8d61-4326-8c1b-e03833cd99e3` remained unchanged. Production acceptance passed the desktop/mobile Playwright journey 2/2 across 1440/1366/1280/1120/960/390/320, ten consecutive homepage probes returned HTTP 200, API health was alive, and the live approved-shop feed returned HTTP 200 with two records. No real PII, testimonial, performance, pricing, API, data, auth, tenant, or migration change. |
| DB-029 — Interactive homepage product tour | PR #253 merged as `f9f4f12ddb94fd745bd0310eb4a8361b40ed3e2b`; all six protected CI contexts passed: Migration graph, Frontend checks, Frontend full suite, Backend tests, Backend full suite, and Playwright smoke. Independently tested application SHA `ac18a2adf8a6b083c6f2d7eef389efe9a0b512a4` passed focused 27/27, full frontend 143/143, production build, changed-file ESLint/diff, dedicated Playwright 3/3, functional QA, and Impeccable/Emil review with no P0–P2 findings. Railway production deployment `97733b7e-127a-4e39-87ad-b337bef34b81` and performance deployment `cd9eba54-9954-4f44-882e-e708c3bb7711` succeeded on the merge SHA and passed `/health`; worker deployment `2066ffd9-8d61-4326-8c1b-e03833cd99e3` remained unchanged before and after release. Production acceptance passed DB-029 Playwright 3/3 twice across desktop/mobile and seven widths, including five source-grounded surfaces, Repair Orders and Invoices event routes, keyboard and rapid retargeting, disclosures, no overflow or undersized controls, reduced modes, and clean console/page/network capture. Ten initial homepage probes and 11 observation rounds over 10 minutes returned HTTP 200 with no 502/5xx; production and performance health stayed 200, and the live approved-shop feed returned HTTP 200 with two records. Rollback target was `74cb317a2578c8f1a064c583588418902483ddd5`; no rollback trigger fired. No API, auth, tenant, payment, backend, dependency, worker, data, or migration change. |
| DB-004 — Customer portal mobile closure | PR #255 merged as `b2ceba0bd9c298fc1ff4a3df525f1f8b105a7069`; all six protected CI contexts passed: Migration graph, Frontend checks, Frontend full suite, Backend tests, Backend full suite, and Playwright smoke. Implementation `552f5942f5ef0c8d2fa09bb37fe5ee778a18da4a` received fresh independent QA GO with no findings: portal/title/route tests 16/16, full frontend 145/145, production build, changed-file lint, diff check, and committed mobile Playwright 2/2 passed at 390px and 320px with exact viewport containment, every visible target at least 44×44, correct route-aware titles, working dashboard → active repair/detail → history/paid-detail navigation, and no page/console errors. Railway production deployment `17c333e0-b311-4b2a-a24c-f92dd51e0d1b` and performance deployment `6815f0a9-d2e4-4280-bffa-6e968d5baeb1` succeeded on the merge SHA and passed `/health`; worker deployment `2066ffd9-8d61-4326-8c1b-e03833cd99e3` remained unchanged. Production acceptance used the deployed frontend with a non-mutating authenticated safe fixture and passed Playwright 2/2 twice at 390px and 320px. Five initial portal probes and 11 observation rounds returned HTTP 200; production and performance health stayed 200, and Railway showed no 5xx entries during the release window. Rollback target was `6f7f7e6317bca3409d768158903faad96ce95aca`; no rollback trigger fired. Real auth/tenant runtime remained green from the independent QA gate. No backend, API, auth, tenant, payment, dependency, worker, data, or migration change. |
| DB-027 — Shop-first public homepage | PR #249 merged as `c31d91de`; every required and informational CI job passed. Independent QA and Impeccable finish review returned GO with no P0–P2 findings. Railway production deployment `c919050e-d583-4a48-b39c-1070354a4838` and performance deployment `fb386e4c-c6bd-483d-83dd-dfdf4d6d730a` succeeded on the merge SHA while worker deployment `2066ffd9-8d61-4326-8c1b-e03833cd99e3` remained unchanged. Production acceptance passed the 320/390 Playwright journey, ten consecutive homepage probes, API health, and the live approved-shop feed with HTTP 200. No API, data, auth, tenant, or migration change. |
| DB-002 — Paid repair-order conversion export | PR #246, production SHA `2ff3910b`; all six CI contexts green; final Security and QA GO; migration 117 applied on PostgreSQL; API and worker deployments succeeded; worker registered and executed `process_paid_invoice_webhooks` every 10 seconds with zero pending events and registered daily PII retention. Versioned keyring parity and safe batch/lease/time budgets are configured. No tenant receiver was enabled or sent data during release; the first tenant enablement must use its approved receiver and acceptance canary |

## Intake template

Copy this row into Inbox before implementation:

```markdown
| DB-### | User/business outcome | Product & Delivery Lead | Priority | Acceptance criteria needed | Unassigned technical owner |
```

For every active item, add links or identifiers for its branch/PR and record the
last passing automated and runtime evidence in the item or associated issue.

## DB-032 acceptance criteria

- On the public landing page's first render, the header bridge constructs in a
  deterministic sequence—deck, pillars, then arch—while all twelve
  `DieselBridge` letters drop independently from varied heights and delays and
  settle at their existing final positions without layout shift.
- Every transformed letter box remains inside the header's vertical safe region
  and inside its reserved horizontal slot throughout the entrance, with no
  visible in-flight overlap at desktop or mobile widths.
- The motion is a one-time explanatory brand moment. It uses compositor-safe
  transform and opacity animation, an intentional ease-out movement curve,
  and no ambient loop, bounce, or repeated animation after data updates.
- Bridge construction is scoped to the animated header mark. The SVG box stays
  fixed while its paths animate; the footer wordmark, authenticated application
  logos, tenant logos, and all other brand surfaces remain static and unchanged.
- The accessible name remains `Diesel Bridge Network`; the animation introduces
  no duplicate announcement, focus change, interaction delay, or hidden content.
- Reduced-motion users receive at most a brief opacity transition with no
  positional travel. The final mark remains fully legible if CSS animation is
  unsupported or interrupted.
- Desktop and 390/320 mobile layouts retain their current dimensions, touch
  targets, and zero horizontal overflow. No API, auth, tenant, data, backend,
  dependency, worker, or migration change is introduced.
- Focused tests, full frontend tests, production build, changed-file lint/diff,
  and live browser inspection pass before independent QA.

## DB-015 acceptance criteria

- `/api/v1/ws` authenticates only from the existing HttpOnly `access_token`
  cookie. A query credential alone is rejected, and a query value can never
  override the cookie. No bearer/session token appears in the WebSocket URL.
- Browser handshakes require an exact match in the configured Origin allowlist.
  Authentication and authorization reuse the HTTPS legacy, selected customer-
  link, active-tenant, and WorkOS authorities. A WorkOS session requires an
  active, non-deleted `ExternalIdentity(provider="workos")`, an active linked
  principal, and an active, non-deleted
  `TenantMembership(provider="workos")` for the selected tenant. Its explicit
  provider role mapping must match the local `User.role`; an unknown role or
  any provider/local projection divergence fails closed initially and during
  live revalidation.
- Tenant-wide repair, invoice, payment, mechanic, and messaging events are
  available only to the explicit shop-staff roles `garage_owner`,
  `garage_admin`, `receptionist`, and `mechanic`. Driver, fleet-manager,
  platform, customer, and unknown roles never inherit that channel merely by
  being non-customer. A customer requires an exact active
  user/tenant/customer link and can register only on that customer channel.
- JWT expiry, blacklist, token version, user/tenant activity, customer link, and
  WorkOS membership remain valid for the life of the connection. Revocation,
  expiry, or a changed tenant context closes the socket with a generic reason.
- Existing server event payloads and `ping`/`pong` remain compatible. Client
  input is notification-only, size-bounded, and rejects unknown messages;
  connection limits and rate limiting remain enforced with generic close copy.
- Recursive redaction removes credential-shaped fields and query/URL values from
  structured application logs, observability context, persistent error records,
  exceptions, and repo-controlled Uvicorn access/error logging. Untrusted
  correlation headers are accepted only when they match the bounded safe-ID
  format; token-, JWT-, cookie-, overlong-, or control-character-shaped values
  are replaced before logging, echoing, or persistence.
- The frontend opens a queryless socket without reading the access token from
  JavaScript. `4001` may perform one HTTPS refresh recovery; authorization,
  origin, replacement, unsupported, and oversized closes are terminal, while
  rate limiting uses bounded backoff without a reconnect storm.
- No schema migration, ticket store, new secret, production data mutation, or
  event-contract change is introduced. Backend deploys before the queryless
  frontend to avoid a compatibility gap; rollback restores both halves together.
- Focused and full backend/frontend tests, compile/build, lint/diff, one Alembic
  head, authenticated browser coverage, secret-marker capture across console,
  API/proxy/observability logs, cross-tenant delivery denial, a fresh independent
  Architecture security GO, independent QA GO, CI, deploy, and canary are required.

## DB-004 acceptance criteria

- Authenticated customer portal dashboard, active repair, repair detail, estimate
  action, history list, and paid history detail render without clipping or hidden
  horizontal overflow at both 390px and 320px.
- The portal brand/home link, repair-detail back control, every history filter,
  and every other visible interactive target measure at least 44 by 44 CSS pixels
  at both required mobile widths.
- Portal routes expose an accurate customer-portal document title and never retain
  the staff-login title after authentication or client-side navigation.
- Customer login, active-repair/action-required state, detail/history navigation,
  keyboard focus and activation, unauthenticated denial, other-customer denial,
  and cross-tenant customer/order denial remain green.
- Focused portal tests, production build, changed-file lint/diff, and a real
  390/320 browser journey pass with no console, page, or failed-request errors.
  Independent QA must return GO before the focused PR can merge and release.

## DB-030 acceptance criteria

- At 390px and 320px, dashboard `Select & pay` and `Pay`, invoice-detail
  `Download PDF`, and `Review payment options` each measure at least 44 by 44
  CSS pixels; committed Playwright scrolls the full dashboard and invoice detail
  and measures every named control.
- Literal CSS pixel minimums remain effective under the compact portal root font,
  while desktop layout, visible labels, keyboard/focus behavior, payment behavior,
  authorization boundaries, and API/data contracts remain unchanged.
- Neither viewport has horizontal page overflow. Focused and full frontend tests,
  production build, exact/changed-file lint and diff checks, DB-004 mobile
  regressions, and independent QA must pass before merge or release.

## DB-029 acceptance criteria

- Remove the visible “Illustrative sample” and “Fictional repair-order data”
  disclaimer. Keep the example internally safe without adding visible legalistic
  copy, customer claims, testimonials, performance claims, or real PII.
- The curated rail order, labels, and Lucide icons are exactly Repair Orders /
  `ClipboardList`, Customers / `Users`, Shop Work / `Wrench`, Invoices /
  `FileText`, and Vehicle History / `History`. These are keyboard-operable tabs;
  selection replaces the entire miniature and is exposed semantically.
- Each miniature is faithful to its current product source rather than an
  invented generic dashboard: Repair Orders uses the repair-order workspace,
  invoice, work/labor, total, and history anatomy; Customers uses the real list,
  detail, Overview, and History patterns; Shop Work uses Shop Cockpit, Work Queue,
  Queue/Activity, and its three lanes; Invoices uses the embedded invoice-card
  states; Vehicle History uses vehicle details and Repair History rows.
- There is no global Intake/Estimate/Approval/Invoice/Payment stage rail and no
  module-by-stage Cartesian state. Each miniature exposes only authentic local
  controls: repair-order history/invoice/work evidence, customer Overview/History,
  Shop Work Queue/Activity and order cards, invoice selection/expansion, and
  vehicle-history row selection/expansion. Per-module local state is preserved.
- Fixtures are typed, deeply frozen, locally imported, internally reconciled,
  and use masked/safe values. Preview interaction performs no network request,
  auth/tenant read, WebSocket, storage write, or mutation and never mounts the
  authenticated production pages or their data hooks.
- The module route begins at the selected rail control and terminates at the
  context sheet. An event route appears only for an authentic selected row, card,
  disclosure, or history event and terminates at its evidence sheet. Routes and
  nodes never cross text, controls, or card interiors. Geometry comes from live
  rendered anchors, inflates obstacles by at least 8px, remeasures after resize,
  fonts, expansion, or content change, and suppresses any invalid route.
- The authored focal motion is the selected event travelling through its route
  into a capability-based morphing evidence sheet. Module-screen morph, glass
  depth, pointer-down feedback, focus, and CTA material motion stay subordinate,
  interruptible, and latest-selection-safe; there is no ambient loop or repeated
  generic rise. The CTA uses layered DieselBridge copper/orange material rather
  than a flat generic orange rectangle.
- Context and optional event sheets use functional Apple-style glass with clear
  edge, depth, translucency, hierarchy, and restrained specular response. If a
  module has no authentic secondary selection, the event sheet collapses rather
  than inventing evidence.
- Desktop keeps the product and its external evidence legible as one composition.
  At widths below 1200px routes are absent and evidence stacks in semantic order.
  At 390/320 the rail becomes a wrapping grid and authentic tables become compact
  rows/cards. The page has no horizontal overflow at 1440, 1366, 1280, 1120, 960,
  390, or 320 CSS pixels; visible interactive targets are at least 44px.
- Keyboard operation, focus visibility, reduced motion, reduced transparency,
  contrast, coarse-pointer behavior, 200% zoom, rapid retargeting, empty/optional
  data, late fonts, missing observers, and rotation/resize are verified without
  stale selection, focus, sheet, or route state.
- Dynamic replacement uses one visually hidden `role="status"` live region with
  polite, atomic announcements. A module change announces the selected module and
  screen; an authentic local selection announces its module, selected control or
  record, and evidence title. Initial render, focus-only movement, resize, and
  geometry/motion changes do not announce. A cancelable 120ms trailing update,
  guarded by the latest transition epoch, ensures rapid input announces only the
  final committed state with no stale or repeated intermediate message.
- Scope is frontend presentation only. No API, auth, tenant, payment, backend
  data, WebSocket, worker, route, or migration change is part of this outcome.
- Focused unit tests, full frontend tests, production build, lint/diff checks,
  source-fidelity and fixture invariants, responsive Playwright geometry and
  interaction coverage, independent QA, and an independent Impeccable/Emil
  finish review must pass before release.
- Release evidence must record the focused PR/merge SHA, required CI contexts,
  both Railway web deployment IDs and health gates, changed homepage canary, and
  proof that the worker deployment remained unchanged.

## DB-035 architecture contract and acceptance

- Scope covers the authenticated staff Dashboard, Customers, Repair Orders,
  Messages, My Shop, and Profile/Settings surfaces. The source-grounded public
  landing language informs hierarchy, typography, material, spacing, product
  identity, and motion; authenticated workflows remain source-faithful rather
  than becoming marketing replicas.
- **Shape confirmed 2026-08-12.** The new presentation is an operating-system
  restructure, not a cosmetic reskin: a permanent desktop product rail, a
  task-first Dashboard, and connected repair/customer/work/invoice/history
  context. The existing old/new presentation boundary remains the rollback
  mechanism; one router and the live operational data remain canonical.
- Rollout is controlled by a tenant/user-scoped old/new presentation flag with
  explicit precedence and instant rollback. Both modes use one router and the
  same routes, APIs, permissions, mutations, auth, tenant boundaries, business
  logic, WebSocket behavior, data, and operational/financial semantics. The flag
  may select presentation components or tokens only and must not fork domain
  state or introduce duplicate navigation trees.
- Architecture defines the flag owner, response shape, bootstrap behavior,
  caching, stale/offline fallback, tenant default versus user override,
  observability, compatibility window, and removal plan. Missing, malformed, or
  unavailable flag data fails safely to the current legacy presentation.
- DieselBridge remains the product identity in the authenticated shell. A tenant
  logo is subordinate shop context and cannot displace the product wordmark,
  navigation identity, accessible name, or browser/document identity.
- Appearance separates immutable brand/semantic tokens from personalization.
  User accent choices never recolor success, warning, destructive, financial,
  authorization, payment, or operational-risk states. Architecture publishes
  the token ownership map and prohibits component-local semantic overrides.
- Accent and theme choices come from a curated accessible set, not arbitrary
  colors. Every supported light/dark/high-contrast combination preserves text,
  icon, border, focus-ring, selected, disabled, and interactive-state contrast;
  forced-colors mode remains usable without relying on authored color alone.
- Appearance revisits accent, font family, font size, density, notification
  placement, theme/mode, live preview, reset, and persistence. Font choices are
  bounded, product-safe families. Compact, default, comfortable, and large are
  explicit type-and-density contracts with documented token values—not global
  transforms or browser-scale tricks.
- Every font/density combination preserves financial column alignment, table and
  work-queue comprehension, truncation/wrapping, 200% zoom, iPad/mobile layouts,
  and at least 44px visible interactive targets. Large type cannot shrink the
  available hit area or create horizontal page overflow.
- Appearance preference ownership is user-scoped server state for cross-device
  sync, with a small local bootstrap/cache for first paint. Architecture defines
  request/response fields, authorization, tenant isolation, validation/errors,
  concurrency or last-write semantics, cache versioning, hydration order, and
  failure behavior. No preference may be read from or written to another user or
  tenant.
- The compatibility plan inventories current localStorage appearance keys,
  defines a one-time validated migration into the server preference, prevents
  repeated overwrites, supports old clients during rollout, and removes obsolete
  keys only after the compatibility window. Reset restores the effective product
  or tenant default on every device and updates preview, cache, and server state
  consistently.
- Live preview is reversible and does not persist until the user applies it.
  Cancel restores the last committed appearance; reset and apply have explicit
  pending, success, error, and retry behavior. Notifications do not cover focal
  controls, move focus unexpectedly, or expose private preference payloads.
- The old and new presentations preserve semantic landmarks, headings, tab and
  disclosure behavior, labels, focus order, keyboard operation, live-region
  behavior, reduced motion, reduced transparency, high contrast, forced colors,
  coarse pointer support, and stable focus across flag or appearance changes.
- Acceptance covers every supported appearance combination on Dashboard,
  Customers, Repair Orders, Messages, My Shop, and Settings at desktop, iPad,
  390px, and 320px, plus 200% zoom, reduced motion, high contrast, and forced
  colors. The matrix verifies no overflow, overlap, hidden actions, undersized
  targets, broken tables, misaligned money, unreadable statuses, stale theme
  flashes, or notification obstruction.
- Representative Playwright journeys run in both old and new presentation modes
  against identical fixtures and assert identical routes, requests, permissions,
  mutations, and resulting business state. Appearance persistence is verified
  across reload, new tab, device/session bootstrap, user switch, tenant switch,
  offline/cache fallback, reset, and rollback.
- Architecture has recorded the versioned flag and preference contract, token
  map, localStorage migration inventory, fixtures, compatibility/removal plan,
  and work split. **Frontend & UX owns the confirmed reshape in
  `codex/db035-authenticated-presentation`**; independent Security reviews
  preference and tenant isolation, and independent QA plus Impeccable/Emil
  finish review gate the rollout.

## DB-HC001 acceptance criteria

- Every modified or untracked path is assigned to a coherent outcome, identified
  as generated/local-only material, or explicitly deferred without deletion.
- Branch and PR state is reconciled against GitHub; merged work is not reshipped
  and unmerged intentional work is not lost.
- Migration history has one Alembic head and the application starts against the
  expected schema.
- Backend, frontend, Playwright, security, tenant-isolation, payment/integration,
  and build checks required by the actual diff pass with fresh evidence.
- Independent Architecture, Security, QA, and pre-landing review gates are GO.
- Intentional work is committed in reviewable scope, pushed through PR and CI,
  merged, deployed, and verified with health/canary evidence and rollback notes.
- The primary repository ends clean. Remaining future work exists only as named
  board items, not unexplained local changes or ambiguous feature chats.

## DB-021 acceptance criteria

- Both Railway web services using root `railway.json` (`diesel-bridge-network`
  and performance `truck-pit-stop`) wait for HTTP `200` from `/health` before a
  new deployment becomes active and receives production traffic.
- A new instance has up to 300 seconds to complete legitimate startup; failure
  to become healthy in that window fails the deployment instead of replacing
  the last healthy instance.
- Automated coverage fails if the API health-check path or timeout is removed or
  changed, and `railway.json` validates against Railway's published JSON schema.
- There is no application behavior, API/data contract, migration, frontend, or
  worker deployment change in this item. Regression enforcement stays outside
  `backend/**` so the worker watch pattern is not triggered.
- Release evidence records both web deployment IDs/SHAs, successful Railway
  health gates, API health, and absence of connection-refused responses during
  each cutover; the worker is not redeployed.
